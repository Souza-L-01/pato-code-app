<% @languages.each do |language| %>
  <%= link_to cl_image_path image.key, height: 100, width: 100, crop: :fill, language_path(language) %>
  <%= link_to cl_image_path image.key, height: 100, width: 100, crop: :fill, language_path(language) %>
  <%= link_to cl_image_path image.key, height: 100, width: 100, crop: :fill, language_path(language) %>
<% end %>
