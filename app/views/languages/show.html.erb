<div class="screen2">
  <ul class="icons2">
    <div class="d-flex justify-content-center align-items-center">
      <div class="lines"></div>
      <div class="title-wrapper px-4">
        <h1 id="lang-title"><%= @language.name%></h1>
      </div>
      <div class="lines"></div>
    </div>
    <div class="d-flex flex-wrap justify-content-center">
      <% @tracks.each do |track| %>
        <div class="col-3 mb-4" >
          <%= link_to language_track_path(@language, track), class: 'link-dark' do %>
            <div class="d-flex flex-column align-items-center">
              <div>
                <%= image_tag 'folder.svg' %>
              </div>
              <div >
                <p class="lang-title"><%= track.title %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <div>
<div>